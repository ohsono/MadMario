# Python >= 3.13 REQUIRED: gym-super-mario-bros >= 8.0 and nes-py >= 9.0
# (the gymnasium-API releases this codebase depends on) only publish wheels
# for Python 3.13+. On older Pythons pip resolves to 7.4.0 and fails.

# Core RL
torch>=2.0
numpy>=1.24
gymnasium>=0.29
gym>=0.26
gym-super-mario-bros>=9.0
nes-py>=9.0

# Image processing
scikit-image>=0.21
opencv-python-headless>=4.8
matplotlib

# Observability
wandb>=0.15

# CLI
typer>=0.9

# MCP integration
mcp>=1.0

# Dev / test
pytest>=8
pytest-cov
